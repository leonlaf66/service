enum CommentPageType {
  yellowpage
  news
}

type CommentPage {
  id: ID!
  rating: Float
  results: CommentPagination
}

type Comment {
  id: ID!
  rating: Float
  user: Member
  content: String
  created_at: Date
}

type CommentPagination {
  total: Int
  items: [Comment]
}

type Query {
  find_comments(page_type: CommentPageType = yellowpage, type_id:ID!, first:Int = 15, skip:Int = 0) : CommentPage
}

type Mutation {
  created_comment (page_id: CommentPageType, type_id: ID!, content: String, rating: Float) : Comment
}